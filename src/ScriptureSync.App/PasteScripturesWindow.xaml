<Window x:Class="ScriptureSync.App.PasteScripturesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Paste Scriptures"
        Height="430"
        Width="590"
        MinHeight="360"
        MinWidth="520"
        WindowStartupLocation="CenterOwner"
        Background="#F5F7FA">
    <Grid Margin="26">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="18" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock FontSize="24" FontWeight="SemiBold" Foreground="#18212F" Text="Paste Scriptures" />
        <TextBlock Grid.Row="1"
                   Margin="0,8,0,14"
                   Foreground="#5D6878"
                   Text="Enter one scripture item per line. Include a translation on each line." />
        <TextBox x:Name="ScriptureTextBox"
                 Grid.Row="2"
                 Padding="12"
                 AcceptsReturn="True"
                 AcceptsTab="False"
                 VerticalScrollBarVisibility="Auto"
                 TextWrapping="Wrap"
                 FontFamily="Consolas"
                 FontSize="14"
                 SpellCheck.IsEnabled="False" />

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="90" Height="38" Margin="0,0,10,0" IsCancel="True" Content="Cancel" />
            <Button Width="120" Height="38" IsDefault="True" Click="Add_Click" Content="Add to Draft" />
        </StackPanel>
    </Grid>
</Window>
